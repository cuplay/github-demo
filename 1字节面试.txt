1.自我介绍
2.项目经历，细节 有什么难点怎么解决的 和别人的技术有什么区别
3.求一个数的平方根编程 时间复杂度多少
    定义一个最小值0和最大值number，把一个数取一个中间值（0+number）/2，然后平方，如果平方大于该数值，就把中间值赋给最大值，否者就把中间值赋给最小值，一直循环，直到取到想要的精度为止；O(logn)
4.浏览器输入一个url会出现什么
  DNS域名解析、建立TCP链接、发起HTTP请求、接受响应结果、浏览器解析html、浏览器布局渲染
5.三次握手 ；四次挥手 
       TCP建立连接的过程：1.客户端向服务端发送连接请求报文段，该报文段包含自身的数据通讯初始序号，告诉对方什么样序号的报文段能被正确接收。请求发送后，客户端进入syn-sent状态。2.服务端接收到连接请求的报文段后，如果同意链接会发送一个应答，该应答包含自身的数据通讯初始序号，发送完成后进入syn-received状态。3.当客户端收到连接同意后，会向服务端发送一个确认报文便进入established状态；服务端收到报文后也进入established状态，此时连接成功。 
       如果是两次握手：a.服务器无法知道自己的序号是否被确认; b.不能防止失效的请求报文段被服务器接收而出现错误（如客户端发送连接请求，但请求报文丢失而未收到确认，于是重传连接请求后收到确认建立了连接；数据传输完成后就释放了连接。在此之后，先前丢失的报文段只是在某些网络节点滞留时间长了，现在到达服务端，此时服务端误以为是新的连接请求便发送确认报文建立新连接，而客户端忽略服务端的确认不发送数据，导致服务端一直等待，出现资源浪费）

       TCP断开连接的过程：1.客户端认为数据发送完成，向服务端发送连接释放请求。2.服务端收到释放请求后告诉应用层要释放TCP连接，发送ACK进入close-wait状态（TCP为双向连接，此时服务端不再接受客户端的数据，但可以发送数据给客户端）3.服务端此时继续发送剩下的数据，完毕后向客户端发送连接释放请求，进入last-ack状态。4.客户端收到释放请求后想服务端发送确认应答，此时客户端进入time-wait状态持续2msl（报文段最大生存期）,若该时间段内没有服务端的重发请求就进入closed状态，当服务端收到确认应答后也进入closed状态。


5.数据库索引是什么 底层是什么实现？ 事务的特点
    物理的对数据库表中一列或多列的值进行排序的一种存储结构；B-树 ；ACID,原子性(Atomicity)、一致性(Correspondence)、隔离
性(Isolation)、持久性(Durability)。
6.tcp的可靠传输是靠什么实现的 怎么处理丢包等错误
    TCP/IP可靠传输的基础是滑动窗口协议和连续ARQ协议，配合着流量控制和拥塞控制，使得整个传输过程保证；TCP为了防止丢包，就给每个报一个序号，同时序号也保证了接收端按序接收，然后接收端实体对已成功的字节回复一个确认（ACK），如果发送端实体再合理的往返时延（RTT）内未收到确认，那么会假设丢失进行重传。
7.反问，后端开发用go
   